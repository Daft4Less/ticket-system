apiVersion: apps/v1
kind: Deployment
metadata:
  name: pagos-stub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pagos-stub
  template:
    metadata:
      labels:
        app: pagos-stub
    spec:
      containers:
        - name: pagos-stub
          image: danielaag5/pagos-stub:v1
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: pagos-stub
spec:
  selector:
    app: pagos-stub
  ports:
    - port: 8000
      targetPort: 8000
